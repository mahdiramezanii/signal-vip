import 'package:flutter/material.dart';

class ForgetPasswordScrean extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("فراموشی رمز عبور")),
    );
  }
}
